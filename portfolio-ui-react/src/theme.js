const { createMuiTheme } = require("@material-ui/core");

const THEME = createMuiTheme({
    palette: {
      secondary: {
        main: "#E33E7F",
      },
      primary: {
        main: "#3ca2c5",
      },
    },
  });

  export default THEME;